const NotFoundPage = () => (
        <h1>404: Page Not Found!</h1>
);

// we can remove the return statement and the curly braces when our statment is very simple like the one here 

export default NotFoundPage;